import { select_config } from "@/plugin/utils/types/select.interface";

export default {
	contain: false,
	flow: "right",
	outline: true,
	center: false,
	hue: true,
	theme: "auto",
	indicator: true,
} as select_config;
